<p id="notice"><%= notice %></p>

<h1>Encomendas</h1>

<table>
  <thead>
    <tr>
      <th>Remetente</th>
      <th>Data</th>
      <th>Funcionario</th>
      <th>Morador</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @encomendas.each do |encomenda| %>
      <tr>
        <td><%= encomenda.remetente %></td>
        <td><%= encomenda.data %></td>
        <td><%= encomenda.funcionario_id %></td>
        <td><%= encomenda.morador_id %></td>
        <td><%= link_to 'Show', encomenda %></td>
        <td><%= link_to 'Edit', edit_encomenda_path(encomenda) %></td>
        <td><%= link_to 'Destroy', encomenda, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Encomenda', new_encomenda_path %>
