<h1>Usuários</h1>

<table>
  <thead>
  <tr>
    <th>Nome</th>
    <th>Tipo</th>
  </tr>
  </thead>

  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.nome %></td>
      <td><%= link_to 'Exibir', users_show_path(:id => user.id)  %></td>
      <td><%= link_to "Deletar", users_deletar_path(:id => user.id), data: { confirm: "Tem certeza?" } %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Voltar', root_path %>
