<p id="notice"><%= notice %></p>

<h1>Usuários</h1>

<table>
  <thead>
  <tr>
    <th>Nome</th>
    <th>Tipo</th>
  </tr>
  </thead>

  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.nome %></td>
      <td><%= link_to 'Mostrar', users_show_path(:id => user.id)  %></td>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Voltar', root_path %>
